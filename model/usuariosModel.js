//Conecta na database
//const db = 

const listar = (from) => {
    //Listar todos os registros da tabela "from" do banco 
}

const criar = (from, dados) => {
    //Criar um novo registro na tabela "from" no banco de dados
}

const atualizar = (from, dados) => {
    //atualizar um registro da tabela "from" no banco
}

const remover = (from, dados) => {
    //remover um registro da tabela "from" no banco
}

const buscar = (from, dados) => {
    //buscar um resgistro na tabela from com o id especificado em dados no banco
}

module.exports = {
    listar,
    criar,
    atualizar,
    remover,
    buscar
}